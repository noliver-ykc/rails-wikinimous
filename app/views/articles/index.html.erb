<h1>Index page</h1>
<%= link_to "Create a new Article", new_article_path %>
<% @articles.each do |article| %>
  <p><%= link_to article.title, article_path(article) %><%= link_to "🗑", article_path(article.id), method: :delete, data: { confirm: "Are you sure?" } %></p>
<% end %>
